{
    "name": "zheng-shangyou-server",
    "version": "1.0.0",
    "description": "A WebSocket server for the Zheng Shangyou poker game.",
    "main": "server.js",
    "scripts": {
      "start": "node server.js",       // Starts the WebSocket server
      "dev": "nodemon server.js",     // Starts the server in development mode (requires nodemon)
      "test": "echo \"No tests specified\" && exit 0"
    },
    "dependencies": {
      "ws": "^8.13.0"                 // WebSocket library for the server
    },
    "devDependencies": {
      "nodemon": "^2.0.22"            // Optional: Automatically restart the server during development
    },
    "author": "Your Name",
    "license": "MIT"
  }